import 'package:flutter/material.dart';

class BabyExercises extends StatelessWidget {
  const BabyExercises({super.key});

  @override
  Widget build(BuildContext context) {
   return Scaffold(
   );
  }
}
